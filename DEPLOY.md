# 🚀 Инструкция по запуску Breathing

## 🖥️ 1. Локальный запуск (Тестирование)

> ⚠️ **Важно:** Просто открывать `index.html` в браузере не рекомендуется, так как браузеры блокируют загрузку файлов (CORS), и приложение не сможет загрузить `techniques.json`. Используйте один из методов ниже.

### Способ А: VS Code Live Server (Рекомендуемый)
1. Установите расширение **Live Server** в VS Code.
2. Откройте `index.html`.
3. Нажмите кнопку **Go Live** в правом нижнем углу VS Code.

### Способ Б: Через Node.js (npx)
Если у вас установлен Node.js, выполните в терминале в папке с проектом:
```bash
npx serve .
```
Откройте ссылку, которая появится в терминале (обычно `http://localhost:3000`).

### Способ В: Через Python
Если у вас установлен Python:
```bash
python -m http.server
```
Откройте `http://localhost:8000`.

---

## 🌐 2. Деплой (Публикация в Интернет)

Чтобы приложение работало в Telegram, оно должно быть доступно по **HTTPS**.

### Вариант 1: GitHub Pages (Бесплатно и надежно)

#### Метод "Для новичков" (через браузер)
1. Зарегистрируйтесь на [GitHub](https://github.com/).
2. Создайте новый репозиторий: [github.com/new](https://github.com/new).
   - **Repository name**: например, `breathing-app`.
   - **Public**: выберите этот пункт.
   - Нажмите **Create repository**.
3. На странице репозитория нажмите ссылку **uploading an existing file**.
4. Перетащите в окно браузера файлы вашего проекта:
   - `index.html`
   - `styles.css`
   - `app.js`
   - `audio.js`
   - `techniques.json`
   - `PROJECT.md`
5. Нажмите зеленую кнопку **Commit changes**.
6. Перейдите в **Settings** (Настройки) репозитория → **Pages** (слева в меню).
7. В разделе **Build and deployment** -> **Branch** выберите `main` (или `master`) и нажмите **Save**.
8. Подождите 1-2 минуты. Обновите страницу. Сверху появится ссылка:
   `https://ваш-ник.github.io/breathing-app/`
   **Скопируйте эту ссылку.**

#### Метод "Профи" (через Git)
```bash
# Инициализация
git init
git remote add origin https://github.com/ваш-ник/breathing-app.git

# Отправка файлов
git add .
git commit -m "Initial deploy"
git branch -M main
git push -u origin main
```
Далее настройте Pages в настройках репозитория (см. пункт 6 выше).

---

## 📱 3. Создание Telegram Mini App

1. Откройте бота [@BotFather](https://t.me/BotFather) в Telegram.
2. Создайте нового бота (если нет): `/newbot`.
3. Создайте приложение: отправьте команду `/newapp`.
4. Выберите вашего бота.
5. Заполните данные:
   - **Title**: Breathing
   - **Description**: Дыхательные практики
   - **Photo**: (можно пропустить)
   - **Web App URL**: Вставьте ссылку, полученную на этапе 2 (GitHub Pages), например: `https://user.github.io/breathing-app/`
   - **Short name**: например, `app`.
6. Готово! Ваше приложение доступно по ссылке: `t.me/ВашБот/app`.

### Добавление кнопки "Меню" (слева от поля ввода)
Чтобы у пользователей была удобная кнопка **"Запустить"** или **"Открыть приложение"** прямо в чате:

1. В @BotFather отправьте команду `/mybots`.
2. Выберите вашего бота.
3. Нажмите **Bot Settings** (Настройки бота).
4. Нажмите **Menu Button** (Кнопка меню).
5. Нажмите **Configure Menu Button**.
6. Отправьте ссылку на ваше приложение (ту же, что использовали выше, например `https://user.github.io/breathing-app/`) и текст кнопки, например "Дышать" или "Открыть".
   *Альтернативно, просто выберите пункт, чтобы кнопка открывала ваш настроенный Web App.*

---

## 🛠️ Решение проблем

**Проблема:** Приложение не загружается, вечная загрузка.
**Решение:** Проверьте консоль браузера (F12). Если видите ошибку CORS или 404 для `techniques.json`, убедитесь, что файл `techniques.json` лежит в той же папке, что и `index.html`, и вы запускаете через локальный сервер (Live Server/npx), а не просто открыли файл.

**Проблема:** Нет звука.
**Решение:** Автовоспроизведение звука запрещено браузерами без действий пользователя. Звук включится только после первого клика по экрану (например, при выборе техники).

---

## 🤖 4. Запуск Telegram Бота (Backend)

Приложение (Frontend) работает на GitHub Pages, но для **рассылок, напоминаний и сбора базы пользователей** нужно запустить Python-бота.

### 📋 Предварительные требования
1.  Установленный **Python 3.8+** ([Скачать](https://www.python.org/downloads/)).
    *   *При установке обязательно поставьте галочку "Add Python to PATH"*

### ⚙️ Установка и настройка

**1. Настройка окружения**
В папке с проектом создайте файл `.env` (можно просто переименовать или скопировать `.env.example`).
Внутри укажите ваши данные:
```ini
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11  # Токен от @BotFather
ADMIN_ID=12345678                                     # Ваш цифровой ID (можно узнать у @userinfobot)
```

**2. Установка зависимостей**
Откройте терминал в папке проекта и выполните:
```bash
pip install -r requirements.txt
```

### ▶️ Запуск

Запустите бота командой:
```bash
python bot.py
```

Если всё прошло успешно, вы увидите сообщение: `Bot started!`.
Теперь бот работает, собирает пользователей в базу (`users.db`) и шлет напоминания.

### 👮 Команды администратора (только для вас)
*   `/broadcast Текст сообщения` — отправить рассылку всем пользователям бота.
    *   *Пример: `/broadcast Вышла новая дыхательная техника! Заходите проверить.`*
*   `/stats` — показать сколько людей нажало /start.

### 👤 Команды для пользователей
*   `/start` — приветствие и кнопка запуска приложения.
*   `/set_time 09:00` — установить время ежедневного напоминания.
*   `/feedback Текст` — отправить отзыв разработчику (вам).

---

## ☁️ 5. Деплой на VPS (Ubuntu)

Инструкция для тех, кто арендовал сервер.

### 🖥️ Какой сервер выбрать?
*   **ОС:** Ubuntu 22.04 или 24.04 (Стандарт).
*   **CPU:** 1 ядро (достаточно).
*   **RAM:** 512 МБ - 1 ГБ (бота хватит с головой).
*   **Диск:** 10 ГБ SSD.
*   **Цена:** ~150-300 руб/мес (Timeweb, Aeza, FirstVDS и др).

### 🛠️ Команды для настройки (копируйте по очереди)

**1. Обновление сервера и установка программ**
```bash
apt update && apt upgrade -y
apt install python3 python3-pip git -y
```

**2. Скачивание бота**
```bash
# Клонируем репозиторий (замените ссылку на свою!)
git clone https://github.com/donecloud/breathing-app.git

# Заходим в папку проекта
cd breathing-app
```

**3. Установка зависимостей**
```bash
# Заходим в папку бота
cd bot

# Устанавливаем библиотеки
pip3 install -r requirements.txt
```

**4. Настройка конфига**
Создайте файл .env внутри папки bot:
```bash
nano .env
```
*Вставьте туда содержимое (BOT_TOKEN=... ADMIN_ID=...). Нажмите Ctrl+O, Enter, Ctrl+X для сохранения.*

**5. Пробный запуск**
```bash
python3 bot.py
```
*Если бот запустился (Bot started!) — нажмите Ctrl+C, чтобы остановить его. Теперь настроим вечную работу.*

### 🤖 Настройка автозапуска (Systemd)

Чтобы бот работал вечно и сам вставал после перезагрузки сервера.

1.  **Создайте файл службы:**
    ```bash
    nano /etc/systemd/system/breathing.service
    ```
2.  **Вставьте этот текст** (проверьте путь `/root/breathing-app/bot`):
    ```ini
    [Unit]
    Description=Breathing Bot
    After=network.target

    [Service]
    User=root
    # ВАЖНО: Путь теперь включает папку bot
    WorkingDirectory=/root/breathing-app/bot
    ExecStart=/usr/bin/python3 /root/breathing-app/bot/bot.py
    Restart=always

    [Install]
    WantedBy=multi-user.target
    ```
3.  **Запустите бота:**
    ```bash
    systemctl daemon-reload
    systemctl start breathing
    systemctl enable breathing
    ```

**Полезные команды:**
*   `systemctl status breathing` — статус бота.
*   `systemctl restart breathing` — перезагрузить (после обновления кода).
*   `git pull` — обновить код (выполнять в папке бота).
